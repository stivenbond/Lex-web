# Archived Legacy Agent Specs

These files were moved out of `.agents/` during repo cleanup because they were overlapping with canonical module and `docs/` specs.

Keep them only as historical reference while comparing prior agent outputs.

Do not update these files for active development. Update the canonical spec locations listed in [../spec-canonical-map.md](../spec-canonical-map.md) instead.
