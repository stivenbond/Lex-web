# Legacy Agent Artifacts

The `.agents/` directory is retained only for compatibility with older agent workflows.

Canonical sources now live in:

- `.codex/skills/lex-platform-delivery/`
- `docs/README.md`
- `docs/project/implementation-status.md`
- module-local specs under `src/Modules/`

Legacy spec content has been reconciled into canonical locations and archived under `docs/project/legacy-agent-specs/`.
