# Legacy workflow notice

Use `.codex/skills/lex-platform-delivery/` and place new specs in their canonical locations:

- module-local specs under `src/Modules/<ModuleName>/`
- cross-cutting specs under `docs/specs/`
