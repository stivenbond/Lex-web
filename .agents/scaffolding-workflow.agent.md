# Legacy workflow notice

This workflow has been superseded by `.codex/skills/lex-platform-delivery/`.

Use the repo-local skill plus the canonical docs under `docs/` and `src/Modules/` instead of this legacy platform-specific scaffold.
