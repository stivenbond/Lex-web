# Legacy entry point

The canonical documentation map now lives in `docs/README.md`.

Repo status and implementation truth now live in `docs/project/implementation-status.md`.

This file is retained only so older references do not break.
